# VidiSnap Environment Configuration
# Copy this file to .env and adjust values for your setup

# Backend URL for local development (outside Docker)
# Use this when running Streamlit locally but backend in Docker
VIDISNAP_BACKEND_URL=http://localhost:8000

# Timeout for video processing (seconds)
VIDISNAP_TIMEOUT=120 